services:
  postgres:
    image: postgres:16-alpine
    env_file:
      .env
    ports:
      - "5440:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskflow"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  user-service:
    build: ./user-service
    ports:
      - "3001:3001"
    env_file:
      .env
    environment:
      OTEL_SERVICE_NAME: ${USER_SERVICE_OTEL_SERVICE_NAME}
    depends_on:
      postgres:
        condition: service_healthy

  task-service:
    build: ./task-service
    ports:
      - "3002:3002"
    env_file:
      .env
    environment:
      OTEL_SERVICE_NAME: ${TASK_SERVICE_OTEL_SERVICE_NAME}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  notification-service:
    build: ./notification-service
    ports:
      - "3003:3003"
    env_file:
      .env
    environment:
      OTEL_SERVICE_NAME: ${NOTIFICATION_SERVICE_OTEL_SERVICE_NAME}
    depends_on:
      redis:
        condition: service_healthy

  api-gateway:
    build: ./api-gateway
    ports:
      - "3010:3000"
    env_file:
      .env
    environment:
      OTEL_SERVICE_NAME: ${API_GATEWAY_OTEL_SERVICE_NAME}
    depends_on:
      - user-service
      - task-service
      - notification-service

  frontend:
    build: ./frontend
    ports:
      - "5180:80"
    depends_on:
      - api-gateway

volumes:
  postgres_data:
